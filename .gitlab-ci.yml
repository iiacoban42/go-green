image: maven:latest

stages:
  - build
  - test
  - checkstyle

build:
  stage: build
  script:
    - mvn compile

test:
    stage: test
    script:
      - mvn test

checkstyle:
    stage: checkstyle
    script:
      - mvn checkstyle:checkstyle